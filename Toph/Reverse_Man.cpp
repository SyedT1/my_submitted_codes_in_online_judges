#include<bits/stdc++.h>
#define all(a) (a).begin(),(a).end()
using namespace std;
using ll = long long int;
string k;
void rev(){
	getline(cin,k);
	reverse(all(k));
	cout<<k<<"\n";
}
int main(){
	rev();
}
